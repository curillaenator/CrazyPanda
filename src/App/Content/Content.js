import { Route } from "react-router-dom";

const Content = () => {
  return (
    <>
      <Route path="/" render={() => {}} />
      <Route path="/" render={() => {}} />
      <Route path="/" render={() => {}} />
    </>
  );
};
export default Content;
